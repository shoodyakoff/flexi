# ── OS / editor ─────────────────────────────────────────────────────────
.DS_Store

# ── Secrets ─────────────────────────────────────────────────────────────
.env

# ── Python ──────────────────────────────────────────────────────────────
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/

# ── Local agent / tool state (but DO ship the bundled skill) ────────────
.claude/*
!.claude/skills/
.codex/
.superpowers/
.trae/

# ── Generated renders, caches, local DBs ────────────────────────────────
output/
raw/*
!raw/README.md
data/*.sqlite*
data/*.sqlite-*
data/*.bak*

# ── Personal 3-strip episode configs (ship only the example) ────────────
pipelines/three_strip/episodes/*.conf
!pipelines/three_strip/episodes/example.conf

# ── Internal planning notes (kept local, not for the public repo) ───────
docs/superpowers/

# ── Media: never commit binary video/audio (root, raw/, assets/, anywhere) ─
# This is the safety net — the engine assets we DO ship (fonts, _meta.json,
# pronunciation.yaml) are text/config, so they pass through untouched.
*.mp4
*.MOV
*.mov
*.m4v
*.mp3
*.wav
*.aac
*.m4a

# ── Asset media folders: keep the folder skeleton (.gitkeep) but ignore
#    any clips/sidecars dropped into them. Tracked engine assets live in
#    assets/fonts/, assets/pronunciation.yaml, assets/hook_overlays/_meta.json
assets/broll/*
assets/broll_brand/*
assets/broll_ingest/*
assets/ctas/*
assets/hooks/*
assets/music/*
assets/sounds/*
assets/talking_head_sources/*
assets/hook_overlays/*
*.transcript.json
*.transcript.json.hash
!assets/**/.gitkeep
!assets/hook_overlays/_meta.json
!assets/**/_meta.json

# Августовский контент-план Сопровод (личные материалы, не в публичный репо)
assets/сопровод08/
видео-финал/
ДЕДУП_ИТОГИ_И_ДОРАБОТКИ.md

# Локальный мусор и медиа (не в публичный репо)
.snap_tmp.*
.req_tmp.*
assets/shnurok_test/

# Личные контент-батчи под мои серии (для себя, не для общего доступа)
pipelines/publish/
pipelines/fun_final.py
pipelines/prep_parts.py
pipelines/schedule_month.py
pipelines/sovety_batch.py
pipelines/sovety_reel.py
pipelines/qa_sovety.py
pipelines/qa_dedup.py
pipelines/qa_seams.py
pipelines/hook_demo_batch.py
pipelines/hook_demo_cta.py
